{
  "name": "auth-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provision Keycloak as an OIDC bridge between a web-only company IdP and CLI clients",
  "scripts": {
    "provision": "node scripts/provision.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
